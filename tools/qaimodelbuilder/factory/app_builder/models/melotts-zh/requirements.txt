# MeloTTS-ZH Full NPU - Python Dependencies
# Platform: Windows ARM64 (Snapdragon X Elite)
# Python: 3.13 ARM64
#
# Install: pip install -r requirements.txt
# Note: qai_appbuilder requires a separate ARM64 wheel (not on PyPI)

# NPU inference runtime (install from local wheel)
qai_appbuilder>=2.45.0

# Array operations
numpy>=1.26

# Chinese G2P (text-to-phonemes)
jieba>=0.42
pypinyin>=0.50
cn2an>=0.5

# English G2P (for Chinese-English mixed text)
g2p_en>=2.1
