"""Infrastructure layer for ``qai.dependency_approval``.

Currently empty — the in-memory adapter lives in ``adapters/`` because
it does not depend on external infrastructure (no SQLite, no HTTP).
This package exists to preserve the standard 4-layer directory layout.
"""
